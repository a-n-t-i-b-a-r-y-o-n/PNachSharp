<ui:Page x:Class="PNachSharp.AddCodePage"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:PNachSharp"
         xmlns:ui="http://schemas.modernwpf.com/2019"
         mc:Ignorable="d" 
         d:DesignHeight="450" d:DesignWidth="800"
         Title="AddCodePage">

    <Grid x:Name="BaseGrid">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="InputBox" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" />
        <Button x:Name="AddRawButton" Grid.Row="1" Grid.Column="0" Click="AddRawButton_OnClick">Add RAW</Button>
        <Button x:Name="AddEncButton" Grid.Row="1" Grid.Column="1">Add Encoded</Button>
    </Grid>
</ui:Page>
